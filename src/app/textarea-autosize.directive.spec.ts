import { TextareaAutosizeDirective } from './textarea-autosize.directive';

describe('TextareaAutosizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaAutosizeDirective();
    expect(directive).toBeTruthy();
  });
});
